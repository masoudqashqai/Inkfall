// =====================================================================================
//  BACKDROP LIBRARY — the "settings". Each registers itself on the Noir engine by name,
//  so a scene names one with `backdrop: { type, ...options }`. Importing this module
//  registers every backdrop (side-effect imports).
// =====================================================================================
import './skyline.js';
import './alley.js';
import './rooftop.js';
import './room.js';
